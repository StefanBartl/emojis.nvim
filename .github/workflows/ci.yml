name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  stylua:
    name: stylua (format check)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: JohnnyMorganz/stylua-action@v5
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          version: v2.5.2
          args: --check lua TESTS

  luacheck:
    name: luacheck (lint)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      # Plain Lua 5.4, not LuaJIT: luacheck only needs *an* interpreter to run
      # on, and which dialect it lints for is set by `std = "luajit"` in
      # .luacheckrc — independent of the host. Pinning a LuaJIT build here buys
      # nothing and breaks when that exact tarball disappears upstream (the
      # commonly copied "luajit-2.1.0-beta3" now 404s).
      - uses: leafo/gh-actions-lua@v10
        with:
          luaVersion: "5.4"
      - uses: leafo/gh-actions-luarocks@v4
      - run: luarocks install luacheck 1.2.0
      - run: luacheck lua TESTS

  tests:
    name: tests (headless nvim) (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    timeout-minutes: 15
    strategy:
      # One platform failing must not cancel the others: which of the three
      # broke is the whole information content of this job.
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    defaults:
      run:
        # Windows runners default to PowerShell; one shell on all three keeps
        # the run step itself from being what differs between platforms.
        shell: bash
    steps:
      - uses: actions/checkout@v5
        with:
          path: emojis.nvim
      # lib.nvim is a real runtime dependency (lib.nvim.usercmd.composer, the
      # :Emojis command layer) as of the composer migration, not just a
      # lazily required optional extra -- checked out as a sibling so
      # `rtp+=../lib.nvim` matches the local dev convention documented in
      # TESTS/README.md.
      - uses: actions/checkout@v5
        with:
          repository: StefanBartl/lib.nvim
          path: lib.nvim
          ref: ci-verified
      # ui.nvim: overlay_spec.lua's `overlay.open("grid")` case actually
      # opens the float, which needs a real ui.kit.select -- ui.kit moved out
      # of lib.nvim into this separate repo in the 2026-09 migration.
      - uses: actions/checkout@v5
        with:
          repository: StefanBartl/ui.nvim
          path: ui.nvim
          ref: ci-verified
      - uses: rhysd/action-setup-vim@v1
        with:
          neovim: true
          version: stable
      - name: Run TESTS suite
        working-directory: emojis.nvim
        run: |
          exec > >(tee -a "$RUNNER_TEMP/test-output.log") 2>&1
          nvim -n -i NONE --headless -u NONE -c "set rtp+=.,../lib.nvim,../ui.nvim" -c "luafile TESTS/run.lua" -c "qa!"
      - uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: test-output-tests-${{ matrix.os }}
          path: ${{ runner.temp }}/test-output.log
          if-no-files-found: ignore
          retention-days: 14
